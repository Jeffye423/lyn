{
    "builds": [
      {
        "src": "dist/**/*", // 指向你的 dist 目录
        "use": "@vercel/static"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/index.html" } // 单页应用需要此项
    ]
  }
  